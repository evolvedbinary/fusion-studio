# Docker Build Environments for Fusion Studio

To build the Docker build environment:

```
$ cd centos7_x64
$ docker build --rm --force-rm --squash -t evolvedbinary/fusion-studio:centos7_x64-be .
```
